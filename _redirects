# Vazio de proposito.
#
# A tentativa de bloquear /migrations e /CLAUDE.md por aqui NAO funcionou:
# no Cloudflare Pages, quando o caminho corresponde a um arquivo real, o
# arquivo estatico vence e as regras deste arquivo nem sao avaliadas.
# Verificado no site no ar: continuavam devolvendo 200 com o conteudo.
#
# O bloqueio vive em functions/_middleware.js, que roda ANTES do arquivo.
